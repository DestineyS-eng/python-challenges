def summation(num):
    #store the sum of numbers
    total=0
    #this will go through the numbers from 0 to whats in num
    for i in range (0,num):
        # i is the number thats presented so i+1 will be the number as it starts at 0 
        total=total+(i+1)
    return total
        
    
